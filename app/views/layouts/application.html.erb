<!DOCTYPE html>
<html>
  <head>
    <title>World Wine Web</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>

  <body>
   <nav>
      <h1><span class="w">W</span>ORLD WINE WE<span class="b">B</span></h1>

      <% if @current_user.present? %>
        Welcome, <%= @current_user.name %>!
        ||
        <%= link_to 'Logout', login_path, method: 'delete' %>
      <% else %>
        <%= link_to 'Login', login_path %>
        ||
        <%= link_to 'Sign Up', new_user_path %>
      <% end %>
      ||
      <a href="/">Home</a>
      <br><br>
      <%= link_to 'Wine List', wines_path %>
      ||
      <%= link_to 'Vino Regions', regions_path %>
      || 
      <%= link_to 'Add a Delicious Wine', new_wine_path %>
      
  
      <hr>
    </nav>

    <%= yield %>

    <br><br>
    <hr>

    <footer>
      &copy; Wine Snob Particulars 2022
    </footer>

  </body>
</html>
